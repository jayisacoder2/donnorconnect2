"use client"

/**
 * ProtectedGate - Wrapper component for protected pages
 * Simply renders children - authentication is handled at the route level
 */
export default function ProtectedGate({ children }) {
  return children
}
